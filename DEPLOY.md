# 🚀 Инструкция по публикации сайта

## Способы публикации

### 1. Vercel (Рекомендуется - БЕСПЛАТНО)

**Самый простой способ!**

1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Установите Vercel CLI:
```bash
npm install -g vercel
```

3. В папке проекта выполните:
```bash
npm run build
vercel
```

4. Следуйте инструкциям в терминале
5. Ваш сайт будет доступен по адресу вида `your-project.vercel.app`

**Или через GitHub:**
1. Загрузите проект на GitHub
2. Войдите на vercel.com
3. Нажмите "Import Project"
4. Выберите ваш репозиторий
5. Готово! Сайт автоматически обновится при каждом коммите

---

### 2. Netlify (БЕСПЛАТНО)

1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Соберите проект:
```bash
npm run build
```

3. Перетащите папку `dist` на страницу Netlify
4. Или подключите GitHub репозиторий для автоматического деплоя

---

### 3. GitHub Pages (БЕСПЛАТНО)

1. Установите пакет:
```bash
npm install gh-pages --save-dev
```

2. Добавьте в `package.json`:
```json
"homepage": "https://ваш-username.github.io/название-репозитория",
"scripts": {
  "predeploy": "npm run build",
  "deploy": "gh-pages -d dist"
}
```

3. Обновите `vite.config.js`:
```javascript
export default defineConfig({
  plugins: [react()],
  base: '/название-репозитория/'
})
```

4. Загрузите проект на GitHub
5. Выполните:
```bash
npm run deploy
```

---

### 4. Обычный хостинг

1. Соберите проект:
```bash
npm run build
```

2. Загрузите содержимое папки `dist` на ваш хостинг через FTP
3. Настройте сервер для работы с SPA (Single Page Application)

**Пример для Apache (.htaccess):**
```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /index.html [L]
</IfModule>
```

---

## Настройка домена

После публикации вы можете подключить свой домен:

### Для Vercel:
1. Перейдите в настройки проекта
2. Domains → Add Domain
3. Следуйте инструкциям по настройке DNS

### Для Netlify:
1. Domain settings → Add custom domain
2. Обновите DNS записи у вашего регистратора

---

## Проверка перед публикацией

✅ **Обязательно:**
1. Замените номер телефона `+79001234567` на ваш
2. Замените `https://t.me/yourusername` на вашу ссылку Telegram
3. Замените временные фото на реальные (из `public/images/`)
4. Проверьте работу всех ссылок
5. Протестируйте на мобильных устройствах

✅ **Рекомендуется:**
1. Добавьте favicon (иконку сайта)
2. Обновите title в `index.html`
3. Добавьте meta description для SEO
4. Настройте Google Analytics (опционально)

---

## SEO оптимизация

Обновите `index.html`:

```html
<head>
  <meta charset="UTF-8" />
  <link rel="icon" type="image/svg+xml" href="/favicon.ico" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  
  <!-- SEO теги -->
  <title>Туры по Северной Осетии | Экскурсии в горы | Трансфер</title>
  <meta name="description" content="Туры и экскурсии по горным ущельям Северной Осетии. Комфортный трансфер на Toyota Alphard. Цейское, Кармадонское ущелье и другие маршруты." />
  <meta name="keywords" content="туры северная осетия, экскурсии горы, трансфер осетия, цейское ущелье, кармадон" />
  
  <!-- Open Graph для соцсетей -->
  <meta property="og:title" content="Туры по Северной Осетии" />
  <meta property="og:description" content="Экскурсии по горным ущельям и комфортный трансфер" />
  <meta property="og:image" content="/images/hero-bg.jpg" />
  <meta property="og:type" content="website" />
</head>
```

---

## Поддержка

Если возникли вопросы:
- Проверьте логи в консоли браузера (F12)
- Убедитесь, что все зависимости установлены (`npm install`)
- Проверьте версию Node.js (должна быть 16+ )

**Удачи с запуском! 🚀**
